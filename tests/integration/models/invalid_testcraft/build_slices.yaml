name: invalid-build-slices-project
version: "0.0"
adopt-info: my-part
platforms:
  amd64:
    build-on: [amd64]
    build-for: [amd64]

# these slices are valid, but top-level build-slices aren't part of the json schema
build-slices:
  - bash_bins
  - base-files_base

parts:
  my-part:
    plugin: nil
