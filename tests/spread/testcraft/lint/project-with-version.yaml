name: lint-with-version
summary: Project with version for post linting.
version: "1.0"

base: ubuntu@24.04
platforms:
  amd64:
    build-on: [amd64]
    build-for: [amd64]
parts:
  nil-part:
    plugin: nil
