name: lint-missing-version
summary: Project missing a recommended version field.

base: ubuntu@24.04
platforms:
  amd64:
    build-on: [amd64]
    build-for: [amd64]
parts:
  nil-part:
    plugin: nil
