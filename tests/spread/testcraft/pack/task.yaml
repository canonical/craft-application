summary: Pack arbitrary testcraft files

environment:
  PROJECT/bare_base: bare-base

execute: |
  cd "${PROJECT}"
  testcraft pack

restore: |
  cd "${PROJECT}"
  test -f "pack-${PROJECT}-$(date -I)-amd64.testcraft"
  rm -f *.testcraft
  testcraft clean
