name: pack-bare-base
summary: Test packing with a bare base.

base: bare
build-base: ubuntu@24.04
platforms:
  amd64:
    build-on: [amd64, arm64, ppc64el, s390x, riscv64]

adopt-info: my-test  # Check that adopt-info works.

parts:
  my-test:
    plugin: nil
    override-build: |
      craftctl set version=$(date -I)
