name: test-test
summary: A testcraft project to test the testcraft test command.
version: "0.1"

base: ubuntu@24.04
platforms:
  platform1:
    build-on: [amd64, arm64, ppc64el, s390x, riscv64]
    build-for: [amd64]
  platform2:
    build-on: [amd64, arm64, ppc64el, s390x, riscv64]
    build-for: [riscv64]

parts:
  my-test:
    plugin: nil
