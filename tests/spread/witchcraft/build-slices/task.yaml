summary: test top-level and part-level build-slices

systems:
  # 20.04 and 22.04 fail when doing a read-only bind mount of the stage dir for the build-slice chroot
  - ubuntu-24.04-64
  - ubuntu-26.04-64

execute: |
  witchcraft pack

restore: |
  witchcraft clean
  rm -f *.witchcraft
