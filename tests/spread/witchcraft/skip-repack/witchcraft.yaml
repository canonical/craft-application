name: repack-test
base: ubuntu@24.04
version: "0.1"
summary: Repack test
description: |
  Repack test

platforms:
  platform-independent:
    build-on: [amd64, arm64, ppc64el, s390x, riscv64]
    build-for: [all]

parts:
  my-part:
    plugin: nil
    source: .
